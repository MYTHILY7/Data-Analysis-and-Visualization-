#include <stdio.h>

int main(void) {
	int t;
	scanf("%d",&t);
	while(t--)
	{
	    int a,b,c;
	    scanf("%d",&a);
	    scanf("%d",&b);
	    c=21-(a+b);
	    if(c<=10&&c>=1)
	    printf("%d\n",c);
	    else
	    printf("-1\n");
	}
	return 0;
}
